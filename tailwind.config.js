/** honDana uses Tailwind only for layout utilities — all colour and type comes
 *  from the CSS variables defined inside the app's <Styles> block. */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: { extend: {} },
  plugins: [],
};
